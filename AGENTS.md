Please write all shared constants in `constants.py` and not anywhere else so they are easy to read and no constant is duplicated. Import them with `from constants import *`.

Keep the comment for functions as Explanations, Args, Returns.
